# 版本记录

本项目使用语义化版本号 `主版本.次版本.修订版本`。业务报告、数据库、API 密钥、日志和本地向量索引不进入 Git。

## v0.3.0 - 2026-08-26

### 修改理由

实际审核使用中需要从“发现问题”进一步走向“快速确认问题”：审核人员必须知道文件来自哪个供应商，能够在档案与报告内容之间快速切换，并在导出报告中直接看到问题所在的原页位置。同时，空的审核依据下拉框和串行服务测试会让首次使用者误以为模板规则没有生效或程序卡住。

因此本版本补齐供应商识别、双向档案浏览、原页红框证据和正式 PDF 报告，并改进服务连接测试的可见性与思考模型兼容。

### 新增

- 结合 OCR/文本规则与受证据约束的 LLM 结果自动识别供应商、制造商、材料厂和报告出具机构；多个名称使用 `/` 分隔。
- 供应商名称写入文档档案和审核批次，旧档案在启动时自动安全回填。
- 供应商档案表与下方报告预览双向联动，并继续提供原文件下载。
- 审核记录增加原报告问题位置；能精确匹配时显示红框证据截图。
- 增加精美 PDF 审核报告导出，包含供应商、批次 ID、模板、文件范围、分级汇总、问题详情、整改建议和红框证据。
- PDF 中无法精确定位红框的问题自动省略截图，避免展示误导性页面。
- 审核依据库为空时显示并自动启用模板内置规则。

### 改进与修复

- 一键服务测试并行检查 LLM、Embedding 和 OCR，分别显示耗时和结果。
- LLM 连接测试支持思考模型：收到推理 token 即确认思考模式可用，不再长时间等待完整回答。
- 增强 OpenAI-compatible 响应字段、空结果、错误 JSON、超时和服务端错误提示。
- Embedding 连接测试使用较短超时，避免“一键测试”长期停留在单一服务。
- Windows 启动脚本自动检查并安装 PDF 报告依赖；macOS 继续依据依赖文件哈希自动更新。
- 数据库采用增量字段初始化，不删除现有档案和审核历史。

### 验证

- 40 项自动测试全部通过。
- 使用现有两页 MTR 完成供应商识别、档案双向联动和审核批次显示验证。
- PDF 报告逐页渲染检查通过：共 5 页，摘要、详情、字体、页眉页脚及三个红框证据位置均显示正常；未定位问题未插入截图。
- Streamlit 页面实际下载 Excel 和 PDF 均验证成功。

## v0.2.1 - 2026-08-25

### 修改理由

实际 CUSTOMER MTR 测试表明，旧版仅依赖整份 PDF 的文字总量和正则提取，存在三类会直接影响审核可靠性的问题：

1. 混合型 PDF 的扫描页可能被静默忽略。例如第一页有文字层时，第二页即使完全是扫描图，也不会触发 OCR。
2. PDF 表格被拆成无关联的文本后，`Standard / Result / Sample / Pass` 的行关系丢失，导致未通过数量和空白实测值未被发现。
3. 模板中的自然语言说明被逐行当成“报告必须存在的字段”，产生大量与报告无关的假问题；LLM 当时只负责润色已有结果，无法发现规则引擎遗漏的问题。

因此本版本将审核模板、确定性规则和 LLM 专家复核重新分层，并要求所有 AI 新增问题必须带有可在原文件对应页验证的证据。

### 新增

- 内置“CUSTOMER 紧固件质量文件审核”模板，新电脑初始化数据库时自动创建。
- 增加独立的专家审核说明字段，与结构化必检项目分开保存。
- 增加 PDF 表格行提取，保留项目、标准、实测、Sample 和 Pass 的对应关系。
- 增加 CUSTOMER 文件完整性、COC/COI/MTR、WDC、检测数据逻辑和逐页可读性检查。
- 从文件名识别 8 位或 10 位 WDC，自动移除空格和连字符。
- 增加受证据约束的 LLM 专家复核：证据无法在指定文件和页码中验证时，结论自动丢弃。
- 增加专家审核、WDC 提取和 LLM 证据验证测试。

### 修复

- 按页判断是否需要 OCR，不再使用整份文档的总文字量掩盖扫描页。
- OCR 服务不可用时保留已提取内容并标记人工复核，不再让整个审核批次直接失败。
- 允许 OpenAI-compatible LLM 模型名称留空并使用服务端默认模型。
- 修复无 API Key 的本地兼容服务仍发送伪造 Bearer 值的问题。
- 改进 Mac Python 3.11+ 发现和一键启动流程。

### 验证

- 35 项自动测试全部通过。
- 首页、供应商档案、规则模板和审核记录页面均通过 Streamlit 页面加载测试。
- 使用 `Q0045 5305-859240(MTR).pdf` 回归验证，规则可稳定发现 `Head Height`、`Thread Length` 的 Sample/Pass 不一致以及缺少实测值的检验行。

## v0.1.0 - 2026-08-24

- 首个本地发布版本。
- 提供 Streamlit 上传、双文档库、审核批次、服务配置、后台 worker、证据展示和 Excel 导出。
