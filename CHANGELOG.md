# 版本记录

本项目使用语义化版本号 `主版本.次版本.修订版本`。业务报告、数据库、API 密钥、日志和本地检索数据不进入 Git。

## v0.5.3 - 2026-08-30

### 修改理由

本地 OpenAI-compatible 服务在模型名留空时返回 HTTP 422，逐条必检项目串行调用又会把单项等待时间累加，导致审核看似卡在队列或 LLM 阶段。规则模板的大文本框也不便于逐项启停和维护；连续上传时需要更清楚地隔离每一次事件。

### 修复与改进

- 模型名留空时自动读取 `/v1/models` 并选取第一个可用模型；每个正式请求始终携带明确的 `model`，避免 HTTP 422。
- HTTP 400/401/403/404/422 立即返回真实错误，不做无意义重试；每条任务使用可配置超时，超时不再次提交。
- 所有启用的独立必检项支持受控调用，可在系统设置调整为 1–16；LM Studio 单模型根据实测默认使用并发 1。
- 单条任务默认超时延长为 300 秒，输出预算提高到 4096 token，避免思考模型在生成最终 JSON 前耗尽原有 600/900 token。
- 首页增加审核方式选择并保存到独立批次：`自适应审核`对每条规则只调用一次，使用短字段紧凑 JSON、220 token 和关闭思考；`思考模式深度复核`让每条规则完整思考。
- 自动识别 LM Studio 原生 API；自适应模式通过 `/api/v1/chat` 明确发送 `reasoning=off`，避免 Qwen3.5 在 OpenAI-compatible 路由中只生成思考内容而超时。其他 OpenAI-compatible 服务仍沿用原接口。
- 精简自适应输出字段与长度，避免模型生成冗长的 actual、requirement、logic 和 suggestion；真实报告单条紧凑调用可稳定完成。
- 将 LLM 默认并发调整为 1。真实报告对照中，并发 2 与单路总耗时接近（50.4 秒对 51.8 秒），但并发 2 将单条等待拉长到 45–50 秒；单路调用为 23–29 秒，更不容易超时。
- 将新安装的审核温度默认值调整为 0，并同步修改当前本机配置，减少同一报告重复审核时的结论波动和 JSON 格式漂移。
- 修复自适应发现疑点但证据为转述时反复调用模型的问题：证据不能逐字定位时直接降为人工复核，保留疑点但不发布不合格结论、不绘制红框。
- 实测当前 LM Studio `qwen3.5-4b` 仅支持 `reasoning=off/on`，完整 `on` 请求单路 300 秒仍会超时；因此自适应固定使用 `off`，完整思考只保留给用户主动选择的深度模式。
- Qwen3/3.5 通过官方 `chat_template_kwargs.enable_thinking` 硬开关控制思考阶段；其他OpenAI-compatible模型保持通用请求兼容。
- 缩短每条任务携带的报告证据、审核依据和既有问题上下文，减少模型首字延迟及输入 token。
- 规则模板改为表格，每行一个必检项，支持新增、动态编辑、启停、勾选、全选和批量删除；旧字符串列表自动兼容。
- 每次点击开始审核都会创建独立 UUID、独立文件目录、独立任务和上传时模板快照；提交后自动清空上传控件，批次名称精确到秒。
- Windows 启动检查要求 Python 3.11+ 和 Streamlit 1.62+；macOS 依赖变化后自动更新虚拟环境。

### 验证

- 覆盖空模型自动发现、HTTP 422 不重试、三任务真实并发、模板快照和同名文件独立上传。
- 66 项全量自动化测试及开始审核、规则模板 Streamlit 执行检查通过。

## v0.5.2 - 2026-08-29

### 修改理由

从 GitHub 全新解压后一键启动时，网页进程即使正常，后台 worker 仍可能因数据库并发初始化或启动异常而退出。旧启动器只等待网页进程，不会发现或恢复 worker，导致审核任务长期停留在“SQLite 本地任务队列”。

### 修复与改进

- 启动器先完成一次 SQLite 初始化和迁移，再启动网页与 worker，避免干净安装首次启动时并发初始化。
- 启动器每秒监控 worker；意外退出后自动恢复并更新真实 PID，连续失败会在界面明确显示异常。
- worker 独立输出到 `logs/worker.log`，网页输出到 `logs/web.log`，不再静默失败。
- 新增 `.worker-status.json` 运行状态，由审核中心显示在线、恢复中、异常、已停止或失联。
- 审核中心增加“全部任务 Dashboard”，显示任务总数、排队、运行中、已完成、失败/取消和 worker 状态。
- 全任务表显示队列状态、批次阶段、进度、当前操作、调用资源、领取尝试次数和失败原因；点击任务可定位批次详情。

### 真实验收

- 使用 Git 压缩包解压到全新临时目录，在独立端口启动完整 launcher、Streamlit 和 worker。
- 上传生成的真实 PDF 后，任务首次轮询已由 `queued` 进入 `running`，1秒后进入 `completed`，领取次数为1。
- 强制终止临时 worker 后，启动器在4秒内自动创建新 worker，PID 已变化且恢复次数记录为1。
- 57项自动化测试、Python 编译检查和审核中心 Streamlit 执行检查通过。

## v0.5.1 - 2026-08-29

### 修复

- Windows 一键启动的依赖检查不再要求已移除的 ChromaDB，避免新电脑在每次启动时重复执行依赖安装。
- v0.5.0 的逐条独立 LLM 审核逻辑、数据库和界面保持不变。

## v0.5.0 - 2026-08-29

### 修改理由

同一份样例报告在改造通用规则后出现了原先可以发现的问题未被检出，且整份报告一次性调用 LLM 时无法明确知道具体哪条规则执行失败。用户要求撤回未经样例验证的通用版本化规则，并让规则模板中的每一条任务独立调用 LLM，最后完整汇总，便于继续扩展和逐项追溯。

### 新增与改进

- 规则模板改为“一行必检项目 = 一次独立 LLM 判断”；新增一行即可扩展审核任务，不需要修改程序。
- 每条任务独立保存合格、不合格、存疑、不适用或调用失败，以及证据、文件、页码、判断逻辑和置信度。
- 单条超时、连接失败、错误 JSON 或虚假证据只影响当前任务，后续规则继续执行；完成后统一汇总。
- LLM 不合格和存疑结论必须具有可在原页逐字验证的证据；完全缺失必须记录检查范围。
- 审核中心新增可点击的逐条规则汇总表；Excel 增加“逐条规则”工作表，PDF 增加独立规则汇总页。
- 任务执行进度显示“第 X/N 条”和当前规则名称，便于识别耗时位置和调用资源。
- 数据库新增 `rule_evaluations` 表，增量创建，不覆盖现有审核批次和历史问题。

- 修复紧固件模板因显示名称不同而跳过专用审核的问题；不再依赖完整模板名称匹配。
- 撤回版本化通用规则、文档类型门控和对应模板界面，恢复经过同一份报告验证的专用审核路径。
- LLM 复核失败且本地规则没有结果时，明确显示“待人工确认”，不再错误显示“未发现问题”。
- 长报告改为按页分段调用 LLM，兼容 8K 上下文模型并覆盖整份文件。
- 每段 LLM 复核限制为 90 秒和 1600 输出 token；失败不重复提交，避免本地思考模型长时间卡住整个批次。
- 当前审核流程完全取消 Embedding/ChromaDB：依据使用本地关键词、条款和结构化规则检索，减少连接失败与等待。
- 审核中心 Dashboard 改为可点击总数卡片，点击后筛选下方批次表；默认打开最近完成批次。
- 修复取消中的批次在 worker 重启后长期停留“正在停止”，以及未完成批次误显示“未发现问题”。
- 最近两次同报告的本地只读复核均恢复识别 4 项问题/待复核事项。

### 架构调整

- 当前审核流程完全取消 Embedding/ChromaDB 运行依赖；审核依据由 SQLite 本地关键词、条款号和已确认结构化要求检索。
- 撤回版本化通用规则、文档类型门控及其模板界面，保留经过样例报告验证的确定性规则路径。

### 验证

- 独立规则逐条调用、结果汇总、虚假证据拦截、缺失范围证据及单条失败隔离均有自动化测试。
- 同一份紧固件样例报告的专用规则回归测试继续验证 Head Height、Thread Length 等已知问题。
- 全量 55 项自动化测试通过，Python 编译检查和 Streamlit 启动检查通过。

## v0.4.0 - 2026-08-29

### 修改理由

实际审核仍存在字段错列、跨页信息未关联、字段缺失误报和证据截图定位不稳定。审核记录与供应商档案分开也增加了查找成本，并且后台任务缺少停止和安全删除能力。本版本将审核流程升级为证据约束的分层审核，并把批次、文件、问题和人工反馈集中到审核中心。

### 新增与改进

- 字段保存提取方法、置信度、页码和可用的 PDF 坐标；跨页冲突支持多证据，完全缺失使用检查范围卡片。
- 审核依据加入 SQLite FTS5 与 ChromaDB 混合检索，严格限制在当前批次选择的依据内。
- 合并“审核记录”和“供应商档案”为审核中心，增加 Dashboard、批次行选择、问题/文件双向跳转、处理事件和统一导出。
- 加入排队立即取消、运行中安全检查点停止、取消后重新审核。
- 加入30天回收站、恢复和到期后永久清除；共享审核依据不会被联动删除。
- 人工确认、驳回、整改和关闭写入本地反馈集，便于追溯审核处理历史。
- 首次迁移 v0.4.0 前自动保留 `qaqc_v2.db.pre-v0.4.0.bak` 数据库备份。

### 验证

- 48项自动化测试通过。
- SQLite 完整性和外键检查通过。
- Streamlit 入口页面加载无异常，旧数据库可增量迁移且历史批次保留。

## v0.3.1 - 2026-08-26

### 修改理由

macOS 实测出现 Embedding 和 OCR 在其他项目可以访问、但本项目连接测试失败。对比 FOS-RAG 后确认，本项目会先探测向量服务的 `/models`，并对非 Ollama 地址错误地自动追加 `/v1`；此外 macOS 系统代理可能拦截本机或局域网 AI 服务请求。

### 修复

- Embedding 连接测试不再依赖可选的 `/models`，直接调用真实 `/embeddings` 接口生成向量。
- 仅对标准 11434 端口的 Ollama 自动追加 `/v1`；其他服务保留用户填写的真实根路径。
- `localhost`、私有 IP、`.local`、`.lan` 和 Mac/Docker 本机地址自动绕过系统 HTTP 代理。
- OCR 健康检查和 Embedding 测试返回实际请求端点及具体错误，不再只显示笼统的“无法访问”。
- 设置页明确提示修改服务配置后需要先保存，再执行连接测试。

### 验证

- 42 项自动测试全部通过。
- 新增非 `/v1` 向量接口、无 `/models` 向量服务、`.local` MinerU 地址及本机代理绕过测试。

## v0.3.0 - 2026-08-26

### 修改理由

实际审核使用中需要从“发现问题”进一步走向“快速确认问题”：审核人员必须知道文件来自哪个供应商，能够在档案与报告内容之间快速切换，并在导出报告中直接看到问题所在的原页位置。同时，空的审核依据下拉框和串行服务测试会让首次使用者误以为模板规则没有生效或程序卡住。

因此本版本补齐供应商识别、双向档案浏览、原页红框证据和正式 PDF 报告，并改进服务连接测试的可见性与思考模型兼容。

### 新增

- 结合 OCR/文本规则与受证据约束的 LLM 结果自动识别供应商、制造商、材料厂和报告出具机构；多个名称使用 `/` 分隔。
- 供应商名称写入文档档案和审核批次，旧档案在启动时自动安全回填。
- 供应商档案表与下方报告预览双向联动，并继续提供原文件下载。
- 审核记录增加原报告问题位置；能精确匹配时显示红框证据截图。
- 增加精美 PDF 审核报告导出，包含供应商、批次 ID、模板、文件范围、分级汇总、问题详情、整改建议和红框证据。
- PDF 中无法精确定位红框的问题自动省略截图，避免展示误导性页面。
- 审核依据库为空时显示并自动启用模板内置规则。

### 改进与修复

- 一键服务测试并行检查 LLM、Embedding 和 OCR，分别显示耗时和结果。
- LLM 连接测试支持思考模型：收到推理 token 即确认思考模式可用，不再长时间等待完整回答。
- 增强 OpenAI-compatible 响应字段、空结果、错误 JSON、超时和服务端错误提示。
- Embedding 连接测试使用较短超时，避免“一键测试”长期停留在单一服务。
- Windows 启动脚本自动检查并安装 PDF 报告依赖；macOS 继续依据依赖文件哈希自动更新。
- 数据库采用增量字段初始化，不删除现有档案和审核历史。

### 验证

- 40 项自动测试全部通过。
- 使用现有两页 MTR 完成供应商识别、档案双向联动和审核批次显示验证。
- PDF 报告逐页渲染检查通过：共 5 页，摘要、详情、字体、页眉页脚及三个红框证据位置均显示正常；未定位问题未插入截图。
- Streamlit 页面实际下载 Excel 和 PDF 均验证成功。

## v0.2.1 - 2026-08-25

### 修改理由

实际紧固件 MTR 测试表明，旧版仅依赖整份 PDF 的文字总量和正则提取，存在三类会直接影响审核可靠性的问题：

1. 混合型 PDF 的扫描页可能被静默忽略。例如第一页有文字层时，第二页即使完全是扫描图，也不会触发 OCR。
2. PDF 表格被拆成无关联的文本后，`Standard / Result / Sample / Pass` 的行关系丢失，导致未通过数量和空白实测值未被发现。
3. 模板中的自然语言说明被逐行当成“报告必须存在的字段”，产生大量与报告无关的假问题；LLM 当时只负责润色已有结果，无法发现规则引擎遗漏的问题。

因此本版本将审核模板、确定性规则和 LLM 专家复核重新分层，并要求所有 AI 新增问题必须带有可在原文件对应页验证的证据。

### 新增

- 内置“通用紧固件质量文件审核”模板，新电脑初始化数据库时自动创建。
- 增加独立的专家审核说明字段，与结构化必检项目分开保存。
- 增加 PDF 表格行提取，保留项目、标准、实测、Sample 和 Pass 的对应关系。
- 增加紧固件文件完整性、COC/COI/MTR、WDC、检测数据逻辑和逐页可读性检查。
- 从文件名识别 8 位或 10 位 WDC，自动移除空格和连字符。
- 增加受证据约束的 LLM 专家复核：证据无法在指定文件和页码中验证时，结论自动丢弃。
- 增加专家审核、WDC 提取和 LLM 证据验证测试。

### 修复

- 按页判断是否需要 OCR，不再使用整份文档的总文字量掩盖扫描页。
- OCR 服务不可用时保留已提取内容并标记人工复核，不再让整个审核批次直接失败。
- 允许 OpenAI-compatible LLM 模型名称留空并使用服务端默认模型。
- 修复无 API Key 的本地兼容服务仍发送伪造 Bearer 值的问题。
- 改进 Mac Python 3.11+ 发现和一键启动流程。

### 验证

- 35 项自动测试全部通过。
- 首页、供应商档案、规则模板和审核记录页面均通过 Streamlit 页面加载测试。
- 使用 `Q0045 5305-859240(MTR).pdf` 回归验证，规则可稳定发现 `Head Height`、`Thread Length` 的 Sample/Pass 不一致以及缺少实测值的检验行。

## v0.1.0 - 2026-08-24

- 首个本地发布版本。
- 提供 Streamlit 上传、双文档库、审核批次、服务配置、后台 worker、证据展示和 Excel 导出。
